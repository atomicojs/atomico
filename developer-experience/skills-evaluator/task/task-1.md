# Tarea de Testing de Skill Atomico

Por favor, procesa los datos del workspace actual utilizando la skill de Atomico (`.agents/skills/atomico`) y genera las vistas y componentes especificados a continuación en la carpeta `./sandbox/`.

## Componentes a Generar

1. **components/ui-button.tsx**:
    - Nombre: `UiButton`.
2. **components/ui-input.tsx**:
    - Nombre: `CustomInput`.
3. **components/ui-card.tsx**:
    - Nombre: `UserProfileCard`.
4. **components/ui-checkbox.tsx**:
    - Nombre: `CheckboxInput`.

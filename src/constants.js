/**
 * Global constants in the core,
 * Rule: Use this file only to declare constants that cannot be shared from the origin by isolating the module.
 */

export const SymbolFor = Symbol.for;

export const TYPE = SymbolFor("Atomico.Type");
